# /etc/systemd/logind.conf.d/t2.conf — lid closes suspend (s2idle; the
# t2-suspend-fix service handles the module lifecycle). swayidle's before-sleep
# hook locks the session first.
[Login]
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend
HandleLidSwitchDocked=ignore
