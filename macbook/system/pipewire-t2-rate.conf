# /etc/pipewire/pipewire.conf.d/10-t2-rate.conf
# The T2 audio device only does 48 kHz. Pin the graph there so a 44.1 kHz
# client can't flip the device rate and glitch every other stream.
context.properties = {
    default.clock.rate          = 48000
    default.clock.allowed-rates = [ 48000 ]
}
