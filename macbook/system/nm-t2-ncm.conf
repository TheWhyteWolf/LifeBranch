# /etc/NetworkManager/conf.d/99-network-t2-ncm.conf
# Don't auto-create a wired profile for the T2's internal NCM interface
# (renamed to t2_ncm by 99-network-t2-ncm.rules) — it DHCP-fails forever.
[main]
no-auto-default=t2_ncm
