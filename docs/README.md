# Screenshots

Referenced from the top-level README once they exist.

| File | Shows |
|---|---|
| `desktop.png` | tiled windows, the bar, the wallpaper mid-generation |
| `cheatsheet.png` | the `Mod+Slash` shortcuts window |
| `lifeconf.png` | the theming TUI mid-edit |
| `lifelock.png` | the lock screen |
| `lifegreet.png` | the login screen (photograph it, or start greetd from a TTY) |

`Print` is region capture, `Ctrl+Print` whole screen, `Alt+Print` window. PNG,
cropped to the screen, no device frames.
